% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconc_TDcond.R
\name{.core_reconc_TDcond}
\alias{.core_reconc_TDcond}
\title{Core Reconciliation via Top-Down Conditioning for Mixed Hierarchies}
\usage{
.core_reconc_TDcond(
  A,
  mean_upper,
  cov_upper,
  L_pmf,
  num_samples,
  return_type,
  suppress_warnings,
  return_upper = TRUE
)
}
\arguments{
\item{A}{Matrix (n_upper x n_bottom) defining the hierarchy where upper = A \%*\% bottom.}

\item{mean_upper}{Vector of mean forecasts for upper level.}

\item{cov_upper}{Covariance matrix of upper level forecasts.}

\item{L_pmf}{List of PMF objects representing the bottom level base forecasts.}

\item{num_samples}{Number of samples to draw from the reconciled distribution.}

\item{return_type}{Character string specifying return format: 'pmf', 'samples', or 'all'.}

\item{suppress_warnings}{Logical. If TRUE, suppresses warnings about samples outside support. Default is FALSE.}
}
\value{
A list containing:
\itemize{
\item \code{bottom_rec}: List with reconciled bottom forecasts (pmf and/or samples).
\item \code{upper_rec}:  (only if \code{return_upper = TRUE}) List with reconciled upper forecasts (pmf and/or samples).
}
}
\description{
Internal function that performs the core reconciliation logic using top-down conditioning
(TD) for mixed hierarchies. First, upper forecasts are reconciled analytically via conditioning
(if necessary), then bottom distributions are updated through probabilistic top-down procedure
by conditioning on the reconciled upper values.
}
\details{
The function internally:
\enumerate{
\item Identifies the "lowest upper" nodes in the hierarchy.
\item If all uppers are lowest-uppers, samples directly from the upper MVN. Otherwise,
analytically reconciles the upper hierarchy and samples from the lowest level.
\item Reconciles bottom distributions by conditioning on the sampled/reconciled upper values
using the probabilistic top-down algorithm.
\item Discards samples that fall outside the support of the bottom-up distribution.
}
}
\keyword{internal}
